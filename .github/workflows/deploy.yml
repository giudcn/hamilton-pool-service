name: Deploy to Hostinger via FTP

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v4

    - name: Sync files via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER || secrets.HAMILTON_FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME || secrets.HAMILTON_FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD || secrets.HAMILTON_FTP_PASSWORD }}
        server-dir: ./
        local-dir: ./
        exclude: |
          **/.git*
          **/.git*/**
          **/.github/**
